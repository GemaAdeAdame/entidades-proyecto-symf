{% extends 'base.html.twig' %}

{% block title %}Datospersonales{% endblock %}

{% block body %}
    <h1>Datospersonales</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ datospersonale.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ datospersonale.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ datospersonale.apellidos }}</td>
            </tr>
            <tr>
                <th>Fechanacimiento</th>
                <td>{{ datospersonale.fechanacimiento ? datospersonale.fechanacimiento|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ datospersonale.dni }}</td>
            </tr>
            <tr>
                <th>Mascotas</th>
                <td>{{ datospersonale.mascotas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_datospersonales_index') }}">back to list</a>

    <a href="{{ path('app_datospersonales_edit', {'id': datospersonale.id}) }}">edit</a>

    {{ include('datospersonales/_delete_form.html.twig') }}
{% endblock %}
